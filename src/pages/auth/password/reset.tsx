import ResetPassword from '../../../containers/auth/password/reset';
import Title from '../../../utils/components/title';

export default function ResetPasswordPage() {
	return (
		<>
			<Title title="Reset Password" />
			<ResetPassword />
		</>
	);
}
