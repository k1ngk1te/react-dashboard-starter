# ─── Security ────────────────────────────────────────────────────────────────

SECRET_KEY=SECRET_KEY
AUTH_KEY=AUTH_KEY
JWT_EXPIRES=14400                        # seconds — default 4 hours

# ─── CSRF ─────────────────────────────────────────────────────────────────────

CSRF_TOKEN=X-Csrf-Token                  # header name used by the server
CSRF_TOKEN_EXPIRES=14400                 # seconds — omit to use session cookie
# DISABLE_CSRF=1                         # disables CSRF validation — never enable in production

# ─── CORS ─────────────────────────────────────────────────────────────────────

ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5000,http://localhost:8888
# ALLOWED_ORIGINS=*                      # allows all origins — restrict in production

# ─── Rate Limiting ────────────────────────────────────────────────────────────

API_AUTH_LIMITER_EXPIRES=3600            # seconds — window for auth routes (default 1 hour)
API_AUTH_LIMITER_MAX=50                  # max requests per window

API_DEFAULT_LIMITER_EXPIRES=900          # seconds — window for general API routes (default 15 min)
API_DEFAULT_LIMITER_MAX=10               # max requests per window

# ─── Server ───────────────────────────────────────────────────────────────────

NODE_ENV=development
TRUST_PROXY=0                            # set to 1 when behind a reverse proxy
PREVENT_CACHE_ON_GET_AUTH_USER=1         # recommended on Netlify
SERVER_TARGET_PORT=5000
# SERVER_TARGET_URL=http://localhost:5000  # overrides Vite proxy target if set

# ─── Dev / Debug ──────────────────────────────────────────────────────────────

TEST_MODE=1                              # exposes raw error messages and debug info

# ─── Client (Vite) ────────────────────────────────────────────────────────────

PORT=3000
VITE_CSRF_TOKEN=X-Csrf-Token             # must match server-side CSRF_TOKEN
VITE_TEST_MODE=1                         # mirrors server-side TEST_MODE

# ─── Mock / API Toggle ────────────────────────────────────────────────────────

VITE_USE_MOCK=1                          # default 1 (mock data) — set to 0 to use the real external API
# VITE_API_URL=https://api.yourdomain.com  # required when VITE_USE_MOCK=0

# ─── CSRF (Client) ────────────────────────────────────────────────────────────

# VITE_DISABLE_CSRF=1                    # must match server-side DISABLE_CSRF
