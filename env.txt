ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://localhost:5000,http://localhost:8888

API_AUTH_LIMITER_EXPIRES=3600 # in seconds
API_AUTH_LIMITER_MAX=50

API_DEFAULT_LIMITER_EXPIRES=900 # in seconds
API_DEFAULT_LIMITER_MAX=10

AUTH_KEY=AUTH_KEY

CSRF_TOKEN=X-Csrf-Token
VITE_CSRF_TOKEN=X-Csrf-Token

CSRF_TOKEN_EXPIRES=14400 # 4 * 60 * 60 # in seconds

PREVENT_CACHE_ON_GET_AUTH_USER=1

SECRET_KEY=SECRET_KEY

JWT_EXPIRES=14400; # in seconds

TEST_MODE=1

TRUST_PROXY=0

# DISABLE_CSRF=1 # Disables CSRF validation on the server. Never enable in production.

PORT=3000 # Optional
SERVER_TARGET_PORT=5000 # Optional
SERVER_TARGET_URL=http://localhost:5000 # Optional

VITE_CSRF_TOKEN=X-Csrf-Token

VITE_TEST_MODE=1

VITE_USE_MOCK=1
# VITE_API_URL=https://api.yourdomain.com

# VITE_DISABLE_CSRF=1 # Disables CSRF requirements on the client. Must match server-side DISABLE_CSRF.